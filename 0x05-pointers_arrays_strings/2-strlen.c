#include "main.h"
#include <string.h>

/**
 * _strlen - returns lenght of a string
 * @s: string whos length is to be returned
 *
 * Returns: length of string
 *
 */
int _strlen(char *s)
{
	return (strlen(s));
}
