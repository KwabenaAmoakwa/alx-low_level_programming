#include "main.h"
#include <stdio.h>
/**
 * add - adds 2 numbers and prints the result
 * @c: number to be added
 * @u: number to be added
 *
 * Return: result of addition of 2 numbers
 */
int add(int c, int u)
{
int a = c + u;
return (a);
}
